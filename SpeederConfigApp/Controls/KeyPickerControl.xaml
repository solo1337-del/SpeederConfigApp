<UserControl x:Class="SpeederConfigApp.Controls.KeyPickerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="36" d:DesignWidth="300"
             Focusable="True"
             PreviewKeyDown="UserControl_PreviewKeyDown"
             LostFocus="UserControl_LostFocus">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- Dropdown Key Selection or Capture Button -->
        <ComboBox x:Name="KeyCombo"
                  Grid.Column="0"
                  DisplayMemberPath="DisplayText"
                  SelectedValuePath="Code"
                  SelectionChanged="KeyCombo_SelectionChanged"
                  Height="34"
                  VerticalContentAlignment="Center"
                  ToolTip="Select key from list or use 'Record' to detect key press" />

        <!-- Record / Capture Key Button -->
        <ToggleButton x:Name="RecordBtn"
                      Grid.Column="1"
                      Margin="6,0,0,0"
                      Height="34"
                      Padding="10,0"
                      Click="RecordBtn_Click"
                      ToolTip="Click to record next key press automatically">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Ellipse x:Name="RecordDot" Width="8" Height="8" Fill="#ef4444" Margin="0,0,6,0" />
                <TextBlock x:Name="RecordText" Text="Detect Key" FontWeight="SemiBold" FontSize="12" />
            </StackPanel>
        </ToggleButton>

        <!-- Clear Button -->
        <Button Grid.Column="2"
                Margin="6,0,0,0"
                Width="34"
                Height="34"
                Content="✕"
                FontSize="12"
                FontWeight="Bold"
                Click="ClearBtn_Click"
                ToolTip="Clear key (Set to None / 0)" />
    </Grid>
</UserControl>
